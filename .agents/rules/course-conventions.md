---
trigger: always_on
---
# INFI Course Rules

- **Deno**: Use `node:sqlite`. Use Deno-native imports, no NPM. Absolute paths preferred.
- **SQL**: UPPERCASE SQL keywords (`SELECT`, `INSERT`).
- **Style**: Use German for comments. Use `deno fmt` (2 spaces).
